import React from 'react';
import Authentication from './authentication';

class Chat extends React.Component {
  render() {
    return (
      <div>
        <Authentication />
      </div>
    );
  }
}

export default Chat;
